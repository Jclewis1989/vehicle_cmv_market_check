const express = require('express');
const router = express.Router();
// Empty routing file
// Most of this data was put into admins.js and the rest was deleted
// Save it for another future date if this project scales
module.exports = router;

